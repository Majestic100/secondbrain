# secondbrain

My personal knowledge base — an [Obsidian](https://obsidian.md) vault.

## Notes on version control

The Obsidian config folder (`.obsidian/`) is intentionally git-ignored. This
keeps machine-specific settings and — importantly — secrets such as the
**Local REST API** key out of the repository. See `.gitignore` for details.
